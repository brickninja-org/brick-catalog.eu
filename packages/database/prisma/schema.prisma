generator client {
  provider        = "prisma-client"
  output          = "../src/generated/prisma"
  previewFeatures = ["views"]
  engineType      = "client"
}

datasource db {
  provider = "postgresql"
}
